[tool.poetry]
name = "flaskproject"
version = "0.1.0"
description = ""
authors = ["Vladimir <ecrvmal@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
flask = "^2.2.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
